Scraper:
- Add scrape metadata method. that will be called on every requested page.
    + should return values: 
        - discovery_url,
        - current_url,
        - meta_author,
        - meta_description,
        - canonical_url,
        - seo_title, 
